<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirect root and all non-public requests into public/
    RewriteRule ^$ public/ [L]
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
